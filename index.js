/**************************
 * interface Robot {
 *   move(): Promise<boolean>;
 *   rotate(): Promise<void>;
 *   cleanSpot(): Promise<void>;
 * }
 **************************/

async function cleanFloor(robot) {
  // Your code here
}
